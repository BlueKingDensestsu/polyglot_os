# .env.local — PostgreSQL connection settings
# Copy this to .env.local and adjust if needed
# Next.js auto-loads .env.local files

PGHOST=localhost
PGPORT=5432
PGDATABASE=polyglot_os
PGUSER=olivier
PGPASSWORD=
